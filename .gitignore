/vendor
/build
/.phpunit.cache
composer.lock
.DS_Store
.phpunit.result.cache
node_modules

# client/dist is committed on purpose: consumers install this package straight
# from git, where npm reads the checkout as-is and never runs a build.
